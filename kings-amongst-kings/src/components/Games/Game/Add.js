import styled from 'styled-components';

const Add = styled.div`
padding: 10px 10px;
border-radius: 5px;
cursor: pointer;

background: white;
border: 3px solid rgba(0, 0, 0, .2);
`;

export default Add;
