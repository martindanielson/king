import styled from 'styled-components';

const View = styled.div`
padding: 10px 20px;
border-radius: 5px;
cursor: pointer;

background: #6dab52;
color: white;
white-space: nowrap;
font-weight: bold;
border: 3px solid rgba(0, 0, 0, .2);
`;

export default View;
